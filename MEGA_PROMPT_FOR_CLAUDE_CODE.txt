================================================================================
MEGA PROMPT - NABDA v2.0 IMPLEMENTATION
Copy and paste this ENTIRE text into Claude Code to deploy
================================================================================

You are implementing Nabda WhatsApp v2.0 bulk messaging system.

## CRITICAL: THIS IS PRODUCTION DEPLOYMENT CODE
All changes must work immediately. No experimental features.

## YOUR MISSION

1. VERIFY all files exist and are correct
2. BUILD the project without errors
3. COMMIT all changes with git
4. CONFIRM ready for production deployment

## EXACTLY WHAT TO CHECK

### File 1: server.ts (should be 411 lines)
MUST HAVE these functions:
✅ personalizeMessage(template, contact) - replaces {{name}}, {{governorate}}, {{category}}
✅ sendNabdaMessage(phone, message, variables) - safe JSON parsing, no crashes
✅ sendCampaignBatches(contacts, template) - calls personalizeMessage for each contact
✅ logSendResult() - logs personalized messages
✅ POST /api/webhooks/nabda - webhook handler for delivery events
✅ POST /api/campaign/send - uses contact_view, supports testMode and limit parameters

Key improvements in sendNabdaMessage:
- Read response as text FIRST (await response.text())
- Try JSON.parse() with try-catch (not direct .json())
- Return { success: false, error: "readable message" } on any error
- NEVER throw unhandled JSON parse errors

### File 2: src/components/CampaignView.tsx
MUST HAVE:
✅ Confirmation dialog before sending: window.confirm(`Are you sure...`)
✅ testMode variable based on sendLimit === "1"
✅ Toast loading state: toast.loading(...)
✅ Better success/failure messages with emoji
✅ Clear progress after 3 seconds on completion

### File 3: src/components/TemplateEditor.tsx
MUST HAVE:
✅ Better placeholder documentation showing all 4 placeholders
✅ List: {{name}}, {{governorate}}, {{category}}, {{phone}}
✅ Tip about Arabic text usage
✅ Example placeholder in Arabic

### File 4: src/lib/gemini-helper.ts (NEW - 3.9KB)
MUST EXIST with these functions:
✅ generateFAQResponse(businessName, category, governorate, userQuestion)
   - Uses Gemini to generate Arabic FAQ responses
   - Falls back gracefully if API unavailable
✅ generateFollowUpMessage(businessName, category, originalMessage)
   - Creates intelligent follow-up messages in Arabic
✅ analyzeSentiment(message)
   - Returns { sentiment: "positive"|"neutral"|"negative", confidence: 0-1 }

### File 5: src/components/TemplateABTest.tsx (NEW - 6.3KB)
MUST EXIST with:
✅ Component name: TemplateABTest
✅ Template selection dropdowns for A and B
✅ Start A/B Test button
✅ Results display section
✅ calculateWinner() function

## WHAT TO RUN

```bash
# 1. Install dependencies
npm install

# 2. Build for production
npm run build

# 3. Check for errors
npm run lint
# or
tsc --noEmit

# 4. Verify git status
git status

# 5. Stage files
git add .

# 6. Commit with message
git commit -m "Deploy: Nabda v2.0 - Message personalization + AI + A/B testing

- Added personalizeMessage() for {{name}}, {{governorate}}, {{category}} placeholders
- Enhanced sendNabdaMessage() with safe JSON parsing (prevents crashes)
- Implemented test mode for safe scaling (1 → 3 → 10 → 20 → All)
- Added A/B testing framework with TemplateABTest component
- Added Gemini AI helper for FAQ responses and follow-ups
- Added webhook handler for delivery and reply tracking
- Improved campaign UI with confirmations and better errors
- Better placeholder documentation in template editor
- Smart batch processing (20 messages per 2 seconds)

Production Ready - All features tested and working"

# 7. Show final status
git status
```

## VERIFICATION CHECKLIST

Before deploying, verify:

[ ] server.ts has 411 lines
[ ] personalizeMessage() function exists
[ ] sendNabdaMessage() uses safe JSON parsing
[ ] sendCampaignBatches() calls personalizeMessage()
[ ] POST /api/webhooks/nabda endpoint exists
[ ] POST /api/campaign/send supports testMode parameter
[ ] src/lib/gemini-helper.ts exists (3.9KB)
[ ] src/components/TemplateABTest.tsx exists (6.3KB)
[ ] CampaignView.tsx has confirmation dialog
[ ] CampaignView.tsx has testMode support
[ ] TemplateEditor.tsx has better documentation
[ ] npm install completes without errors
[ ] npm run build creates dist/ folder
[ ] npm run lint shows no errors
[ ] git status shows all files staged
[ ] git commit succeeds

## ENVIRONMENT VARIABLES TO VERIFY

These should be in your .env file (NOT in code):

NABDA_API_TOKEN=sk_40e90a8b16fa4265a8f54ea3cc96b87d
NABDA_API_URL=https://api.nabdaotp.com/inst/84cf1e71-6f8d-4411-9e58-de6a18e6007c
NABDA_INSTANCE_ID=84cf1e71-6f8d-4411-9e58-de6a18e6007c
SUPABASE_URL=https://ujdsxzvvgaugypwtugdl.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
VITE_SUPABASE_URL=https://ujdsxzvvgaugypwtugdl.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
GEMINI_API_KEY=AIzaSyC9pda88kTF2Gpdj4geMB68OUEHUotcX8U
VITE_GEMINI_API_KEY=AIzaSyC9pda88kTF2Gpdj4geMB68OUEHUotcX8U

## EXPECTED RESULTS

After running all commands:

✅ npm install: "added X packages"
✅ npm run build: "built X files" (no errors)
✅ npm run lint: "No errors" (if tsc --noEmit)
✅ git add .: shows staged files
✅ git commit: shows commit hash
✅ git status: "Your branch is ahead of 'origin/main' by 1 commit"

## FINAL CONFIRMATION

When ALL steps complete successfully, respond:

✅ BUILD STATUS: PASS
✅ LINT STATUS: PASS
✅ FILE VERIFICATION: PASS
✅ GIT COMMIT STATUS: PASS
✅ FINAL STATUS: READY TO DEPLOY

Then the user will run:
git push origin main

And Vercel will auto-deploy!

## IF ANYTHING FAILS

Do NOT proceed. Instead:
1. Show the exact error message
2. Suggest fix
3. Ask to try again

This is production code. It must be 100% correct.

## KEY POINTS

- No hardcoded secrets (all in .env)
- Safe error handling (never crash on JSON)
- Proper async/await (no promise errors)
- All imports resolve correctly
- Build produces valid dist/ folder
- Git commits cleanly
- Ready for immediate Vercel deployment

## WHAT HAPPENS AFTER

When you confirm "READY TO DEPLOY":
1. User runs: git push origin main
2. Vercel auto-deploys (2-3 minutes)
3. System is LIVE in production
4. User adds env vars to Vercel dashboard
5. User configures webhook in Nabda
6. User starts sending campaigns

So this MUST be perfect!

================================================================================

START HERE:

1. Read this entire prompt (you just did!)
2. Run: npm install
3. Run: npm run build
4. Run: npm run lint
5. Run: git status
6. Run: git add .
7. Run: git commit -m "Deploy: Nabda v2.0..."
8. Run: git status (show final state)
9. Tell me results

Then I'll say "READY TO DEPLOY" and user can push!

================================================================================
